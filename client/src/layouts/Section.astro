---
interface Props {
  id: string;
}

const { id } = Astro.props;
---

<section id={id} class='section'>
  <slot name='title' />
  <slot name='body' />
</section>

<style>
  .section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem;
  }
</style>
